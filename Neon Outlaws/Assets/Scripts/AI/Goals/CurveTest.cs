using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CurveTest : MonoBehaviour
{
 //   public AnimationCurve aniCurve;


 //   public AnimationCurve distance, health, cooldownOne, cooldownTwo, cooldownThree;
 //   public AnimationCurve distW, healthW, cooldownOneW, cooldownTwoW, cooldownThreeW;

	//// Use this for initialization
	//void Start ()
 //   {
		
	//}
	
	//// Update is called once per frame
	//void Update ()
 //   {
 //       float curDist;
 //       float curHealth;
 //       float output = 0.0f, outputWeights = 0.0f;
 //       outputWeights = distW.Evaluate(curHealth) + healthW.Evaluate(curHealth);
 //       output = distance.Evaluate(curDist) + health.Evaluate(curHealth);

 //       output /= outputWeights;

 //       EnemyAIController blah;
 //       blah.setValue(0, output);
	//}
}
